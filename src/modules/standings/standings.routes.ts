import { Router } from 'express';
import * as standingsController from './standings.controller';

const router = Router();

// Note: The main endpoint GET /api/championships/:id/standings is defined in championships routes

export default router;
